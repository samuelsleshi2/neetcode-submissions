class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        ans = []

        for i, item in enumerate(nums):
            ans.append(item)
        for i, item in enumerate(nums):
            ans.append(item)
        

        return ans